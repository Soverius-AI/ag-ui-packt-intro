# Add files here to ignore them from prettier formatting
/dist
/coverage
/.nx/cache
/.nx/workspace-data
pnpm-lock.yaml
.angular

# The demo code is formatted by hand to match the slides and the step branches.
# Format-on-save would reflow it and break `git diff --stat <step>-solution` on stage.
apps/
